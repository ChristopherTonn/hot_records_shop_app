<h1>Orders Index</h1>

<div class="row">
  <% @orders.each do |order| %>
  <div class="col-sm-2 col-md-4">
    <h6>Order # <%= order.id %>:
      <%= link_to order.product.name, order %></h6>
    <p></p>
    <p><%= order.product_id %></p>
    <p><%= order.user_id %></p>
    <strong><%= order.product.price %> €</strong>
  </div><!-- /col -->
  <% end %>
</div>
